LET fac(n) = IF n <= 1.0 THEN 1.0 ELSE n * fac(n-1.0);
LET e = 2.71828182846;
LET pi = 3.14159265359;
//LET pow(a, b) = IF b <= 1.0 THEN a ELSE a*pow(a, b-1.0);
LET ln(x) = 2.0 * (((x-1.0)/(x+1.0)) + (pow(((x-1.0) / (x+1.0)), 3.0)/3.0) + 
                                       (pow(((x-1.0) / (x+1.0)), 5.0)/5.0) + 
                                       (pow(((x-1.0) / (x+1.0)), 7.0)/7.0) + 
                                       (pow(((x-1.0) / (x+1.0)), 9.0)/9.0)); 
//ln(1.55)?
LET sqrt(x) = pow(x, 0.5);
(pi*2.0)?
.